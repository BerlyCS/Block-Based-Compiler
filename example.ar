FUNCION CONFIGURACION()
  ENTERO LED_ROJO = 2;
  ENTERO LED_AMARILLO = 4;
  ENTERO LED_VERDE = 7;
  SALIDA(LED_ROJO);
  SALIDA(LED_VERDE);
  SALIDA(LED_AMARILLO);
FIN

FUNCION PRINCIPAL()
  PRENDER(LED_ROJO);
  ESPERAR(1000);
  APAGAR(LED_ROJO);
  PRENDER(LED_AMARILLO);
  ESPERAR(200);
  APAGAR(LED_AMARILLO);
  PRENDER(LED_VERDE);
  ESPERAR(1000);
  APAGAR(LED_VERDE);
FIN


